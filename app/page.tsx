import Home from "@/components/Home/Home";
import React from "react";

// Homepage route:
// Entry component for the root page.
// Delegates the complete page UI composition to the reusable Home component.
const Homepage = () => {
  return <Home />;
};

export default Homepage;
